import QtQuick
import QtQuick.Controls

// Placeholder screen for camera/3D functionality (Phase 3).
Item {
    id: cameraScreen

    Label {
        anchors.centerIn: parent
        text: "Camera / 3D View\n(Coming in Phase 3)"
        font.pixelSize: 20
        horizontalAlignment: Text.AlignHCenter
        color: "#888888"
    }
}
